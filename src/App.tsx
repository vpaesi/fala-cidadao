import { BrowserRouter, Routes } from 'react-router-dom';

function App() {
  return (
      <BrowserRouter>
        <div className="min-h-screen bg-gray-50">
          <Routes>
          </Routes>
        </div>
      </BrowserRouter>
  );
}

export default App;